import React from "react";
import { commentI } from "../interfaces/postInterface";

export default function Comment({ commentData }: { commentData: commentI[] }) {
  return (
    <div>
      Comment
    </div>
  );
}
